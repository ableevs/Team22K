package rgr;
import static org.junit.Assert.assertEquals;

import org.junit.Test;
public class CoverTest {

	@Test
	public void CoverTest() {
		Cover cover = new Cover();
		String in="name";
		cover.setName(in);
		String out =cover.getName();
		assertEquals(in, out);
	}
}
